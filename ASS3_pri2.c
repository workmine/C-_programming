// Area of Rectangle
#include<stdio.h>
int main ()
{
	int length, breadth;
	printf("Enter the length and breadth is: ");
		scanf("%d %d", &length, &breadth);
		int area;
		area = length * breadth;
		printf("The area is :%d", area);
		return 0;
}

