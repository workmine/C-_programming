// Area of Circle
#include<stdio.h>
int main ()
{
	float radius, area;
	printf("the enter the radius is :");
	scanf("%f", &radius);
	area = 3.141 * radius * radius;
	printf("The Area of the Circle is :%f", area);
	return 0;
}
